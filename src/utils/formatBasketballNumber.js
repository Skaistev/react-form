

function formatBasketballNumber (number){
    return (number < 10?'0':'') + number;


    // const  pad = (num, minSize) => (num + '').padStart(minSize, "0"); pridet zenkla pries skaiciu it t.t.
}

export {formatBasketballNumber}